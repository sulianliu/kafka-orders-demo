# kafka-orders-demo
Pure Java Kafka client demo illustrating core Kafka semantics such as partitioning, ordering, at-least-once delivery, idempotent consumption, and manual offset management. Includes producer, consumer, and in-memory state store with simulated failures.
